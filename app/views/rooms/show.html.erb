<section class="darkgrey_section last_content_section">
  <div class="container">
    <%= react_component("VideoChat", room_name: @session.room_id, user_name: "#{current_user.first_name} #{current_user.last_name}"  ) %>
  </div>
</section>